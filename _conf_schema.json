{
  "max_history": {
    "description": "每个群保留的消息记录条数",
    "type": "int",
    "default": 300
  },
  "active_reply_enable": {
    "description": "是否启用概率主动回复",
    "type": "bool",
    "default": false
  },
  "active_reply_probability": {
    "description": "主动回复触发概率，范围 0.0-1.0",
    "type": "float",
    "default": 0.1
  },
  "active_reply_whitelist": {
    "description": "主动回复白名单群号（留空表示所有群）",
    "type": "list",
    "default": []
  },
  "cached_size": {
    "description": "cached 保留最近多少条消息（按会话）",
    "type": "int",
    "default": 120
  },
  "describe_image_provider_id": {
    "description": "describe_image 使用的 provider_id，留空则使用当前会话 provider",
    "type": "string",
    "_special": "select_provider",
    "default": ""
  },
  "embedding_provider_id": {
    "description": "黑话检索使用的 embedding provider_id，留空则回退为本地哈希向量",
    "type": "string",
    "default": ""
  },
  "describe_image_ask": {
    "description": "describe_image 调用模型时使用的提示词",
    "type": "string",
    "default": "请客观描述这张图片中的主要内容，简洁一些。"
  },
  "image_url_cache_size": {
    "description": "图片 URL 的 LRU 缓存容量（保留最近多少张图片的描述结果）",
    "type": "int",
    "default": 120
  },
  "jargon_recall_count": {
    "description": "每次向 prompt 注入多少条与当前消息相关的黑话",
    "type": "int",
    "default": 5
  },
  "history_inject_count": {
    "description": "每次向 prompt 注入多少条最近历史消息",
    "type": "int",
    "default": 12
  },
  "jargon_max_records": {
    "description": "每个会话范围最多保存多少条黑话",
    "type": "int",
    "default": 500
  },
  "rrf_k": {
    "description": "RRF 混合搜索的平滑常数 (k)，决定了排序权重的衰减速度，默认 60",
    "type": "int",
    "default": 60
  },
  "web_port": {
    "description": "管理界面的 Web 端口，设为 0 则禁用",
    "type": "int",
    "default": 6180
  },
  "mod_api_url": {
    "description": "可选：管理端 API 地址（例如 https://admin.example.com），通过插件配置提供",
    "type": "string",
    "default": ""
  },
  "mod_auth_token": {
    "description": "可选：管理端认证 token，用于 /api/mod/mute 调用",
    "type": "string",
    "default": ""
  },
  "mod_max_mute_duration": {
    "description": "可选：单次禁言时长上限（秒），默认 0 表示不限制",
    "type": "int",
    "default": 0
  }
}
